import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.juliom.app',
  appName: 'myfirstionicproyect',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
